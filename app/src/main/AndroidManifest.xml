<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daysatu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_recycle1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".menu.KantorActivity" />
        <activity android:name=".menu.AboutActivity" />
        <activity android:name=".menu.EmployeeActivity" />
        <activity android:name=".menu.AboutApps" />
        <activity android:name=".menu.DataKaryawan" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".LoginAcivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>